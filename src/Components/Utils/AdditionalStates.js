// ENUMS
export const ReadingLevel = {
  EASY: true,
  HARD: false,
};

Object.freeze(ReadingLevel);

// ENUMS
export const OrientationState = {
  HORIZONTAL: true,
  VERTICAL: false,
};

Object.freeze(OrientationState);
